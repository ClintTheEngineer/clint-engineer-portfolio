Portfolio for ClintTheEngineer built using React via Vite.
Project images, captions, and data are dynamically fetched via API.
Portfolio can be updated via custom built(from scratch) headless CMS solution.
